{
    "version": 2,
    "builds": [
      { "src": "pages/api/**/*.py", "use": "@vercel/python" },
      { "src": "pages/**/*.tsx", "use": "@vercel/next" }
    ],
    "routes": [
      { "src": "/api/(.*)", "dest": "/pages/api/$1" },
      { "src": "/(.*)", "dest": "/pages/$1" }
    ]
  }
  